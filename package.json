{
  "name": "farraen-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Farraen UI theme build (Bronze & Midnight)",
  "scripts": {
    "build:sass": "sass static/scss/main.scss:static/css/main.css --no-source-map --style=compressed",
    "watch:sass": "sass --watch static/scss/main.scss:static/css/main.css --style=expanded"
  },
  "devDependencies": {
    "sass": "^1.66.1",
    "bootstrap": "^5.3.2",
    "bootstrap-icons": "^1.11.0"
  }
}
